import { Pixelify_Sans, Inria_Sans } from "next/font/google";

export const pixelify_Sans = Pixelify_Sans({
    subsets: ["latin"],
    display: "swap",
});

export const inria_Sans = Inria_Sans({
    subsets: ["latin"],
    display: "swap",
    weight: ["400"],
});
